import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Hide the Next.js dev badge — it sits over the UI and shows up in recordings.
  devIndicators: false,
};

export default nextConfig;
