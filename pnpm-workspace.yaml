# Keep the virtual store inside the project: pnpm's global store symlinks
# packages outside the project root, which Turbopack refuses to compile.
enableGlobalVirtualStore: false

allowBuilds:
  esbuild: true
  unrs-resolver: true
  sharp: true
